{
  "name": "whatsapp-webhook-render",
  "version": "1.0.0",
  "description": "WhatsApp webhook for Heroku command triggering - deployed on Render",
  "main": "webhook.js",
  "scripts": {
    "start": "node webhook.js",
    "dev": "nodemon webhook.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}